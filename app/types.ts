export interface Product {
  icon: string;
  name: string;
  desc: string;
}

export interface Step {
  n: string;
  title: string;
  desc: string;
}

export interface Stat {
  num: string;
  label: string;
}